#include <stdio.h>
int main(){
	// int grade1, grade2, grade3,  grade4;
	// float averageGrade, gradeDelta, percentDiff;

	// grade1 = grade3 = 88;
	// grade2 = 79;

	// printf("what did you get on the fourth test.\n");
	// printf("(An integer between 0 and 100.)\n");
	// scanf(" %d", &grade4);

	// averageGrade = (grade4 + grade2+ grade3+ grade1)/4;
	// printf("Your average is %.2f.\n", averageGrade);

	// gradeDelta = 95- averageGrade;
	// percentDiff = 100 *((95-averageGrade)/95);
	// printf("Your grade is %.2f points lower than the  top grade in the class. (95)you are %.2f percent behind", gradeDelta, percentDiff);
	// printf("that grade !\n\n\n");

	float value;
	value = 2 + 4.3;
	printf("%f\n",value );

	return a = (4 < 10);



	return 0;
}